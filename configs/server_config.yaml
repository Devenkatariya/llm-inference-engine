server:
  host: "0.0.0.0"
  port: 8000
  workers: 1               # Keep at 1 for GPU sharing
  reload: false
  log_level: "info"

rate_limiting:
  enabled: false
  requests_per_minute: 60

cors:
  allow_origins: ["*"]
